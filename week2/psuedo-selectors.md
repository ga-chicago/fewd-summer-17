## Psuedo Selectors

```css

.lime-items {
  background: lime;
}
/* selects every odd item in the list */
.lime-items:nth-child(odd) {
  background: olive;
}
/* selects every even item in the list */
.lime-items:nth-child(even) {
  background: chartreuse;
}
```
